kind = "web"
previewPath = "/"
title = "AI Guardian"
version = "1.0.0"
id = "artifacts/ai-guardian"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24870

[services.development]
run = "pnpm --filter @workspace/ai-guardian run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ai-guardian", "run", "build" ]
publicDir = "artifacts/ai-guardian/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24870"
BASE_PATH = "/"
